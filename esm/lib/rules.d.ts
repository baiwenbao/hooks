import { Rule } from 'antd/lib/form';
declare const getRules: (required?: boolean, validate?: string, validateMsg?: string, initialRules?: Rule[], requiredMsg?: string | undefined) => Rule[];
export default getRules;
